const Navigation=(function(){'use strict';let currentSection='gallery';const DOM={sections:document.querySelectorAll('section'),navLinks:document.querySelectorAll('.header__link[data-section]')};function switchSection(sectionId){if(currentSection===sectionId)return;DOM.navLinks.forEach(link=>link.classList.remove('active'));const activeLink=document.querySelector(`.header__link[data-section="${sectionId}"]`);if(activeLink)activeLink.classList.add('active');DOM.sections.forEach(section=>section.classList.remove('section-active'));const targetSection=document.getElementById(sectionId);const gallerySection=document.getElementById('gallery');if(targetSection){if(sectionId==='shop'&&gallerySection&&targetSection!==gallerySection.previousElementSibling){gallerySection.parentNode.insertBefore(targetSection,gallerySection)}
targetSection.classList.add('section-active');currentSection=sectionId;if(sectionId==='shop'){loadShopContent()}}}
function loadShopContent(){Shop.init()}
return{init:function(){DOM.navLinks.forEach(link=>{link.addEventListener('click',function(e){e.preventDefault();const section=this.getAttribute('data-section');switchSection(section);window.scrollTo({top:0,behavior:'smooth'})})});switchSection('gallery');loadShopContent()},getCurrentSection:function(){return currentSection},switchToSection:function(sectionId){switchSection(sectionId)}}})()